# Достижения (Achievements)

Стена значков в Профиле, рядом с уровнем: слева карточка уровня (70% ширины),
справа плитка достижений (30%) с последним полученным значком и счётчиком.
Тап по плитке открывает всю стену; тап по значку — крупный вид, где значок
вращается сам и его можно крутить пальцем.

Стена группируется по темам (заголовок раздела + плитки), а не одним общим
списком — по образцу «Awards» в Apple Fitness: там значки разбиты на «Close
Your Rings», «Monthly Challenges» и т.д., и Apple кодирует категорию не только
цветом, но и силуэтом — круг для одних типов, баннер для других, шестигранник
для третьих. Мы переносим все три сигнала, и держим их независимыми друг от
друга:

- **тир** (bronze/silver/gold) → металл рамки (`.bgcore`/`.bgrim`, как раньше);
- **группа** → и цвет заливки внутри рамки (`.bgaccent`), и силуэт всего
  значка (`--shape`) — то есть у каждой из 7 групп своя форма медальона, не
  только свой цвет.

У шести из семи форм край вогнутый (concave) — изгибается ВНУТРЬ между
вершинами, а не выпирает наружу; это даёт им характер «выштампованной медали»
вместо «наклейки». Исключение — `Работы`/круг: у него концавностей нет вообще,
это сознательно самая нейтральная форма набора (см. таблицу ниже), а не
недосмотр. С этим же приёмом (вогнутость либо её осознанное отсутствие)
каждая форма читается как своя, даже в силуэте, до того как разобрать цвет
или глиф.

## Откуда берутся данные

`achievements(userId)` в `src/db.ts` выводит всё из **реальной истории** —
`generations`, `events`, `ledger`, `users` — а НЕ из `xp_claims`.

Это не стилистическое предпочтение. `xp_claims` заполняется только пока
настроена XP-экономика (`awardXpOnce` читает конфиг первым и выходит, если
награда не задана). Стена, построенная на нём, была бы сегодня пустой и —
хуже — **никогда бы не восстановилась**: реальная история пользователя
навсегда осталась бы вне его профиля. Вывод из первичных таблиц стоит
нескольких дешёвых агрегатов и ретроактивен по построению.

Заблокированные значки тоже возвращаются, с прогрессом. Пустые места — это то,
что придаёт вес полученным, и заодно самое понятное описание возможностей
продукта.

## Группы и набор

Названия — параметрические (число + за что), как у Apple («100 Move Goals»,
«Move Goal 200%»), а не поэтические ярлыки: значение достижения читается по
одному заголовку, без похода в описание.

Форма и цвет заливки — на уровне ГРУППЫ, тир — на уровне отдельного значка.

Цвета — не 7 независимо подобранных hex, а первые 7 слотов из 8-цветной
категориальной темы, провалидированной под тёмную поверхность (skill
`dataviz`, `scripts/validate_palette.js`: полоса светлоты OKLCH L 0.48–0.67,
контраст ≥3:1, худшая соседняя пара по цветовой слепоте ΔE 8.4). Прежние
цвета такими не были: худшая соседняя пара (Мастерство↔Поддержка — они и
правда соседи на стене) давала ΔE 1.8 при дейтеранопии, то есть эти два
цвета были практически неразличимы при красно-зелёной цветовой слепоте.
Перестановка порядка этих 7 (не только замена одного цвета) может тихо
вернуть провал на соседней паре — перепроверяйте валидатором.

| Группа | Цвет заливки | Форма (силуэт) |
|---|---|---|
| Работы | `#3987E5` синий | круг — простая монета, без вогнутостей |
| Форматы | `#D95926` оранжевый | баннер/лента со скруглённым верхом и вогнутой прорезью снизу (ласточкин хвост) |
| Исследование | `#199E70` бирюзовый | шестигранник с вогнутыми (не прямыми) сторонами |
| Мастерство | `#C98500` янтарный | «подушка» — скруглённый квадрат с вогнутыми серединами сторон |
| Поддержка | `#D55181` малиновый | 8-лучевая звезда-розетка — вогнутые лучи, самый «наградной» силуэт |
| Сообщество | `#008300` зелёный | четырёхлепестковый клевер (quatrefoil), вогнутая талия между лепестками |
| Уровень | `#9085E9` фиолетовый | скруглённый треугольник с вогнутыми сторонами |

| Группа | id | Название | Условие | Тир |
|---|---|---|---|---|
| Работы | `first_render` | Первая работа | 1 успешная работа | bronze |
| Работы | `ten_renders` | 10 работ | 10 работ | silver |
| Работы | `fifty_renders` | 50 работ | 50 работ | gold |
| Форматы | `first_video` | Первое видео | 1 видео | silver |
| Форматы | `first_text` | Текст в картинку | 1 картинка по описанию | bronze |
| Форматы | `scenarist` | Первый сценарий | 1 работа из сценария | silver |
| Исследование | `explorer` | 3 модели | 3 разные модели | silver |
| Исследование | `polymath` | 10 моделей | 10 разных моделей | gold |
| Исследование | `stylist` | 5 стилей | 5 разных стилей | silver |
| Мастерство | `uploader` | Первое фото | 1 загрузка | bronze |
| Мастерство | `wordsmith` | Первое улучшение | 1 улучшение промпта | bronze |
| Мастерство | `sharer` | Первая отправка | 1 отправка результата | bronze |
| Поддержка | `first_purchase` | Первое пополнение | 1 покупка | silver |
| Поддержка | `patron` | 3 пополнения | 3 покупки | gold |
| Сообщество | `inviter` | Первый друг | друг создал работу | silver |
| Сообщество | `circle` | 5 друзей | 5 таких друзей | gold |
| Уровень | `level_3` | 3 уровень | уровень 3 | silver |

Порядок групп на стене — как в таблице: Работы → Форматы → Исследование →
Мастерство → Поддержка → Сообщество → Уровень.

## Как выглядит значок

Значок собран из CSS, а не из картинки: он должен быть верным и на 44px в
плитке, и на 190px во вращении, на любом DPI, и стена обязана отрисоваться
целиком на первом кадре — до того, как что-либо скачается.

Слои, изнутри наружу:

- `.bgcore` — металл рамки: конический градиент со смещённым центром, цвет
  зависит от **тира** (`--b1/--b2/--b3`, уже было);
- `.bgrim` — фаска рамки: светлая кромка с освещённой стороны, тёмная с
  теневой — именно это говорит глазу «металл, у него есть толщина»;
- `.bgaccent` — новый слой: заливка внутри рамки цветом **группы** (`--grp`),
  с тем же смещённым бликом, что и у `.bgcore` — рамка и заливка светятся с
  одной стороны, это и держит слои визуально одним объектом;
- `.bgface` — глиф, поднятый по оси Z ещё выше, поэтому при повороте он даёт
  параллакс относительно рамки и заливки (это и продаёт объём);
- `.bgholo` — интерференционный отблеск, широкая диагональная полоса, идущая
  по поверхности.

Силуэт (`clip-path: var(--shape)`) задаётся классом группы (`.grp-output` и
т.д., см. таблицу выше), а не общий на всех — это и есть «форма кодирует
категорию» из брифа. `.badge` сам несёт запасное значение `--shape` (простой
круг), которое группа переопределяет — если у значения `group` из БД однажды
не найдётся своего `.grp-*` (переименование/новая группа без готовой CSS),
значок останется хоть с каким-то силуэтом вместо голого прямоугольника без
клипа.

Все силуэты, кроме круга (см. ниже), — плоские `polygon()` на 20-65 точек
(круг же не полигон вовсе — не путать с «всегда ~100 точек», это неверно и
для полигонов: точность подбирается под форму, шестиграннику нужно меньше
точек, чем розетке-звезде). Это не `clip-path: path()`: у `path()` координаты
в абсолютных px, не в процентах, и он не масштабируется вместе с элементом —
один и тот же значок рисуется и на 44px (плитка), и на 190px (вращение), и на
весь экран (детальный вид), так что силуэт обязан быть в процентах, как и
раньше со щитом. Кривые получены сэмплингом: взять N опорных точек по
вершинам фигуры, у каждого ребра середину сдвинуть к центру (вогнутость) и
сэмплировать квадратичную кривую Безье между вершиной и сдвинутой серединой —
так получается гладкая вогнутая дуга из обычных точек `polygon()`. Круг —
единственное исключение из этой вогнутой семьи (см. врезку выше) и
единственный, что не хранится как `polygon()`: `.badge`'s `aspect-ratio:
1/1.12` не квадратный, а `polygon()`-проценты разрешаются по каждой оси
отдельно, так что равный x/y-радиус даёт эллипс, а не круг; `circle()`
разрешает свой процент от `sqrt(width²+height²)/√2` — единым числом на все
направления — и поэтому остаётся кругом независимо от пропорций бокса.

Всё это — `scripts/badge-shapes.mts` (кроме круга, которому скрипт не нужен):
перегенерировать/подправить форму или добавить 8-ю группу — `npx tsx
scripts/badge-shapes.mts`, вставить нужный `.grp-*` в `public/app.html`
поверх старого, не редактируя координаты руками.

Заблокированный значок сохраняет геометрию (свою форму) и теряет цвет — стена
читается как один набор с дырами, а не как две несвязанные группы.

## Замена глифов на настоящую графику

Глиф рисуется всегда; сверху накладывается `/img/badge-<id>.png` и удаляет
глиф по `onload`. Если файла нет — `onerror` убирает картинку, глиф остаётся.
Поэтому произведённую графику можно добавлять по одному файлу за раз, ничего
не переписывая, и набор никогда не выглядит сломанным.

### Бриф на генерацию (Higgsfield)

Генератор — **Higgsfield MCP** (`generate_image`), модель **`gpt_image_2`**
(`quality: "high"`) как основная; **`nano_banana_2`** ("Nano Banana Pro") —
равноценная альтернатива того же уровня. Не fal.ai (тот аккаунт держит
рендеры для пользователей, а не арт продукта) и не Recraft — оба дают
плоскую векторную картинку, а бриф требует премиального, «мирового класса»
металлического рельефа, как у наградных медалей Apple Fitness.

Тир (bronze/silver/gold) по-прежнему держит материал глифа — на этом слое он
не упразднён рамкой: рамка (`.bgcore`/`.bgrim`) — тот же металл снаружи, а сам
предмет внутри — рельеф из того же металла, поэтому значок читается одним
цельным объектом, а не рамкой одного цвета с произвольной картинкой внутри.
Группа (`.bgaccent`) добавляет цветную эмаль-подложку позади рельефа — это и
есть комбинация «тир + группа», которую просили: два независимых сигнала на
одном значке, не один вместо другого.

Требования, общие для всех значков:

- **Материал по тиру** — bronze: тёплая патинированная бронза; silver:
  полированная сталь с холодным отражением; gold: глубокое золото с тёмными
  гранями (не жёлтый пластик) — премиальный 3D-рельеф, не плоская заливка;
- **Один источник света**, сверху-слева, под тем же углом, что и CSS-фаска;
- **строго по центру, фронтально**, без перспективы — значок будет вращаться
  средствами приложения, и собственная перспектива картинки это сломает;
- **никакой монеты/медальона/рамки вокруг объекта** — рамку и заливку уже
  рисует CSS (`.bgcore`/`.bgrim`/`.bgaccent`); картинка — только сам предмет,
  парящий в кадре, без диска или пластины под ним;
- **прозрачный фон**, PNG, квадрат 512×512, объект занимает ~60% кадра, с
  равными отступами со всех сторон. Ни gpt_image_2, ни nano_banana_2 не
  отдают настоящий альфа-канал по промпту — оба рисуют шахматную сетку как
  пиксели. Обязательный шаг после генерации: Higgsfield `remove_background`
  по `job_id`, затем обрезка по альфа-каналу и повторное центрирование —
  точное позиционирование обеспечивает пост-обработка, а не первый кадр
  генератора;
- **никакого текста** — надпись под значком уже есть в интерфейсе, и она
  локализуется.

Шаблон промпта:

```
<СЮЖЕТ>, sculpted as a small premium 3D relief charm in <МАТЕРИАЛ>, single
soft key light from upper left, subtle iridescent highlight, deep bevelled
surface detail, world-class product-render quality like a premium Apple
Fitness award medal, isolated object floating alone with nothing behind it,
no coin, no medal plate, no disc, no enclosing frame, no border, no badge
shape, no keyring loop, no text, no letters, transparent background, square
composition, centred, object fills roughly 60% of frame
```

`<СЮЖЕТ>` по значкам: камера с блендой (`first_render`), кинолента
(`ten_renders`), колоннада (`fifty_renders`), хлопушка (`first_video`), перо
(`first_text`), раскрытая книга (`scenarist`), компас (`explorer`), скрещённые
молоток и гаечный ключ (`polymath`), палитра с кистью (`stylist`), рамка с
фотографией (`uploader`), искра (`wordsmith`), антенна с волнами сигнала
(`sharer`), билет (`first_purchase`), корона (`patron`), рукопожатие
(`inviter`), кольцо из фигур людей (`circle`), треугольник из трёх граней
(`level_3`).

**Значки не генерируются из пользовательских фотографий.** Загруженные фото
собраны для одной цели — сделать пользователю результат — и повторное
использование их как оформления продукта не покрывается опубликованной
политикой конфиденциальности.
